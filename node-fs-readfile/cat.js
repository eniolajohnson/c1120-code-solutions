const fs = require('fs');

// What I want to do is an accumulator that will take all of process.argvs children and add them together one after the other and hereby returning the accumulator at the end as the data
